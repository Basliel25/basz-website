---
import { getCollection } from "astro:content"
import BaseLayout from "../layouts/BaseLayout.astro"
import MegaHero from "../components/MegaHero.astro"
import StripDivider from "../components/StripDivider.astro"
import IdxCard from "../components/IdxCard.astro"
import IndexRecent from "../components/IndexRecent.astro"

const posts = (await getCollection("blog", ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 5)

const projects = (await getCollection("projects", ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 5)
---

<BaseLayout title="bas_" description="Systems engineer. Writing, projects, notes.">
  <MegaHero
    tag="Systems engineer. Writing in public about the software layer where abstractions stop being free."
    meta={{ Base: "Amsterdam · NL", Work: "Systems & tooling", Stack: "Go · Rust · C · Linux" }}
  />
  <StripDivider text="· INDEX ·" />
  <div class="index-sections">
    <IdxCard
      num="01" kicker="PROJECTS" title="Things I've built"
      desc="Infrastructure, tools, side experiments. Some shipped, some learning exercises."
      href="/projects" count={`${projects.length} indexed`}
    />
    <IdxCard
      num="02" kicker="DISPATCHES" title="Writing"
      desc="Long-form notes on systems, software, and things worth measuring."
      href="/blog" count={`${posts.length} published`}
    />
    <IdxCard
      num="03" kicker="ABOUT" title="One person, in public"
      desc="Who is writing, and from where."
      href="/about" count="Amsterdam · NL"
    />
    <IdxCard
      num="04" kicker="VAULT" title="Notes"
      desc="A slice of the zettelkasten. MOCs, seedlings, evergreens."
      href="https://vault.bas.dev" count="Public notes"
    />
  </div>
  <StripDivider text="· RECENT ·" />
  <IndexRecent posts={posts} projects={projects} />
</BaseLayout>
